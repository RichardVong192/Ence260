/*
 * Use sizeof to find how many bytes of memory a double
 *  variable occupies on the lab computers. What's the answer?
 * [Write your answer as a number, i.e., using just the digits 0 - 9.]
 */

#include <stdio.h>

int main(void){

    int num = 0;
    printf("Size of int is '%zu' bytes of memory\n", sizeof num);
}
